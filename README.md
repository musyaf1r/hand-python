make sure u alr have this in your terminal
virtual environment
.venv\Scripts\activate

Downgrade NumPy ke versi 1.x
pip uninstall numpy
pip install "numpy<2.0"

Reinstall OpenCV 
pip install --force-reinstall opencv-python
